 public void cadastrar(){
    
    agenda.add(new Agendamento());
    //idAgendamento = 0
    
        for (int i = 0; i < agenda.size(); i++) {
        if(agenda.get(i).getIdAgendamento() == 0)
                System.out.println("INFORME O SEU ENDEREÇO");
            agenda.get(i).setEndereco("fdsfdsfds");
            agenda.get(i).especialidade = "jofds";
            System.out.println(agenda.get(i).endereco);
        }
        
    }
    
    public void mostrarTodos(){
    for (int i = 0; i < agenda.size(); i++) {
        System.out.println(agenda.get(i).endereco);
        agenda.get(i).nomeCompleto = sc.nextLine();
    }
    
    }
    
    
    classe paciente - falar com uelbert a respeito do cadastro de paciente 
